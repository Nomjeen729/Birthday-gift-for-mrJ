<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>File System Organizer</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        
        body {
            background: linear-gradient(135deg, #6a11cb 0%, #2575fc 100%);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }
        
        .container {
            width: 90%;
            max-width: 1000px;
            background: rgba(255, 255, 255, 0.9);
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
            overflow: hidden;
        }
        
        header {
            background: #4a6fc7;
            color: white;
            padding: 20px;
            text-align: center;
        }
        
        h1 {
            font-size: 24px;
            margin-bottom: 10px;
        }
        
        .description {
            font-size: 14px;
            opacity: 0.9;
        }
        
        .main-content {
            display: flex;
            min-height: 500px;
        }
        
        .sidebar {
            width: 250px;
            background: #f5f7fa;
            padding: 20px;
            border-right: 1px solid #e0e6ed;
        }
        
        .folder-structure {
            margin-top: 20px;
        }
        
        .folder {
            padding: 8px 12px;
            margin: 5px 0;
            border-radius: 5px;
            cursor: pointer;
            display: flex;
            align-items: center;
            transition: all 0.2s;
        }
        
        .folder:hover {
            background: #e0e6ed;
        }
        
        .folder.active {
            background: #4a6fc7;
            color: white;
        }
        
        .folder-icon {
            margin-right: 8px;
        }
        
        .content {
            flex: 1;
            padding: 20px;
        }
        
        .path {
            font-size: 14px;
            color: #7a869a;
            margin-bottom: 20px;
            padding-bottom: 10px;
            border-bottom: 1px solid #e0e6ed;
        }
        
        .files-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(120px, 1fr));
            gap: 15px;
        }
        
        .file-item {
            text-align: center;
            padding: 15px 10px;
            border-radius: 8px;
            background: white;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
            transition: transform 0.2s;
            cursor: pointer;
        }
        
        .file-item:hover {
            transform: translateY(-5px);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.15);
        }
        
        .file-icon {
            font-size: 32px;
            margin-bottom: 10px;
            color: #4a6fc7;
        }
        
        .file-name {
            font-size: 12px;
            word-break: break-word;
        }
        
        .action-buttons {
            display: flex;
            justify-content: center;
            gap: 10px;
            margin-top: 20px;
            padding: 20px;
            border-top: 1px solid #e0e6ed;
        }
        
        button {
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-weight: 600;
            transition: all 0.2s;
        }
        
        .btn-primary {
            background: #4a6fc7;
            color: white;
        }
        
        .btn-primary:hover {
            background: #3b5aa6;
        }
        
        .btn-secondary {
            background: #f5f7fa;
            color: #4a6fc7;
            border: 1px solid #e0e6ed;
        }
        
        .btn-secondary:hover {
            background: #e0e6ed;
        }
        
        .modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.5);
            justify-content: center;
            align-items: center;
        }
        
        .modal-content {
            background: white;
            padding: 25px;
            border-radius: 10px;
            width: 90%;
            max-width: 400px;
        }
        
        .modal h2 {
            margin-bottom: 15px;
            color: #4a6fc7;
        }
        
        .form-group {
            margin-bottom: 15px;
        }
        
        label {
            display: block;
            margin-bottom: 5px;
            font-weight: 600;
        }
        
        input, select {
            width: 100%;
            padding: 10px;
            border: 1px solid #e0e6ed;
            border-radius: 5px;
        }
        
        .modal-buttons {
            display: flex;
            justify-content: flex-end;
            gap: 10px;
            margin-top: 20px;
        }
        
        @media (max-width: 768px) {
            .main-content {
                flex-direction: column;
            }
            
            .sidebar {
                width: 100%;
                border-right: none;
                border-bottom: 1px solid #e0e6ed;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <h1>File System Organizer</h1>
            <p class="description">Create folders and organize your files efficiently</p>
        </header>
        
        <div class="main-content">
            <div class="sidebar">
                <h3>Folders</h3>
                <div class="folder-structure">
                    <div class="folder active">
                        <span class="folder-icon">📁</span>
                        <span>public</span>
                    </div>
                    <div class="folder">
                        <span class="folder-icon">📁</span>
                        <span>src</span>
                    </div>
                    <div class="folder">
                        <span class="folder-icon">📁</span>
                        <span>documents</span>
                    </div>
                    <div class="folder">
                        <span class="folder-icon">📁</span>
                        <span>images</span>
                    </div>
                    <div class="folder">
                        <span class="folder-icon">📁</span>
                        <span>audio</span>
                    </div>
                </div>
            </div>
            
            <div class="content">
                <div class="path">Home / public /</div>
                
                <div class="files-grid">
                    <div class="file-item">
                        <div class="file-icon">🎵</div>
                        <div class="file-name">bg.mp3</div>
                    </div>
                    <div class="file-item">
                        <div class="file-icon">🖼️</div>
                        <div class="file-name">image.png</div>
                    </div>
                    <div class="file-item">
                        <div class="file-icon">🖼️</div>
                        <div class="file-name">image2.png</div>
                    </div>
                    <div class="file-item">
                        <div class="file-icon">📄</div>
                        <div class="file-name">README.md</div>
                    </div>
                    <div class="file-item">
                        <div class="file-icon">⚙️</div>
                        <div class="file-name">eaint.config.mjs</div>
                    </div>
                    <div class="file-item">
                        <div class="file-icon">📊</div>
                        <div class="file-name">jsonflg.json</div>
                    </div>
                    <div class="file-item">
                        <div class="file-icon">⚙️</div>
                        <div class="file-name">next.config.mjs</div>
                    </div>
                    <div class="file-item">
                        <div class="file-icon">📦</div>
                        <div class="file-name">package.json</div>
                    </div>
                </div>
            </div>
        </div>
        
        <div class="action-buttons">
            <button class="btn-primary" onclick="openModal()">Create New Folder</button>
            <button class="btn-secondary">Move Files</button>
            <button class="btn-secondary">Download All</button>
        </div>
    </div>
    
    <div class="modal" id="modal">
        <div class="modal-content">
            <h2>Create New Folder</h2>
            <div class="form-group">
                <label for="folderName">Folder Name</label>
                <input type="text" id="folderName" placeholder="Enter folder name">
            </div>
            <div class="form-group">
                <label for="parentFolder">Parent Folder</label>
                <select id="parentFolder">
                    <option value="root">Root</option>
                    <option value="public">public</option>
                    <option value="src">src</option>
                </select>
            </div>
            <div class="modal-buttons">
                <button class="btn-secondary" onclick="closeModal()">Cancel</button>
                <button class="btn-primary" onclick="createFolder()">Create</button>
            </div>
        </div>
    </div>
    
    <script>
        function openModal() {
            document.getElementById('modal').style.display = 'flex';
        }
        
        function closeModal() {
            document.getElementById('modal').style.display = 'none';
        }
        
        function createFolder() {
            const folderName = document.getElementById('folderName').value;
            const parentFolder = document.getElementById('parentFolder').value;
            
            if (folderName) {
                alert(`Folder "${folderName}" created successfully in ${parentFolder}`);
                closeModal();
                
                // Clear input
                document.getElementById('folderName').value = '';
            } else {
                alert('Please enter a folder name');
            }
        }
        
        // Close modal when clicking outside
        window.onclick = function(event) {
            const modal = document.getElementById('modal');
            if (event.target == modal) {
                closeModal();
            }
        }
        
        // Add click event to folders
        const folders = document.querySelectorAll('.folder');
        folders.forEach(folder => {
            folder.addEventListener('click', function() {
                folders.forEach(f => f.classList.remove('active'));
                this.classList.add('active');
                
                // Update path
                const pathElement = document.querySelector('.path');
                const folderName = this.querySelector('span:last-child').textContent;
                pathElement.textContent = `Home / ${folderName} /`;
            });
        });
    </script>
</body>
</html>
